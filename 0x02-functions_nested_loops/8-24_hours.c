#include "holberton.h"
/**
 * jack_bauer(void) - calls function
 *
 * Description:
 *
 * Return: 0
 */
void jack_bauer(void)
{
int h, m;

for(h = 0; h <=23; h++)
	_putchar(h);

	_putchar(':');
	for(m = 0; m <=59; m++)
	_putchar(m);

}

int h;
int hh;
int m;
int mm;

	for (h = 0; h <= 23; h++)
	{
		for (hh = 0; hh <= 23; hh++)
		{
			putchar(h);
			putchar(hh);
			if (!(a == 57 && b == 57))
			{
			putchar(44);
			putchar(32);
			}
		}
	}
	putchar('\n');
return (0);
}

