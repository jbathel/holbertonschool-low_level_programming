#include <stdlib.h>
#include <stdio.h>
#include "dog.h"

/**
 * *new_dog - calls function
 * @name: dog name
 * @age: dog age
 * @owner: owner name
 * Description: Function that creates a new dog
 * Return: 0
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	if (d == NULL)
		return;
	printf("Name = %s\nAge = %g\nOwner = %s\n", d->na
			me, d->age, d->owner);
}
